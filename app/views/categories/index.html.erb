<h2>Categories</h2>
<%@categories.each do |category|%>
  <h3><%= category.name%></h3>
  <p><%= category.icon%></p>
<% end %>
<%= button_to "Log Out", destroy_user_session_path, method: :delete, data: { turbo: false }, class:"btn log-out-btn" %>